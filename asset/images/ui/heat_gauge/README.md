# Heat gauge sources

The active combat heat gauge and needle live under
`res://UI/themes/modern/`. Files in this directory whose names end in
`_pixel.png` are superseded experiments and must not be referenced by runtime
UI.
