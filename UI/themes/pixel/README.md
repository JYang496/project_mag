# Legacy pixel UI

This directory contains superseded pixel-art experiments. Runtime UI must not
reference assets from this directory. New HUD assets belong under
`res://UI/themes/modern/` and should use scalable SVG or antialiased drawing.
