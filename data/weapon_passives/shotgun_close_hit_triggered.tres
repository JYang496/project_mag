[gd_resource type="Resource" script_class="WeaponPassiveBranchDefinition" load_steps=2 format=3]

[ext_resource type="Script" path="res://data/WeaponPassiveBranchDefinition.gd" id="1_script"]

[resource]
script = ExtResource("1_script")
passive_id = "shotgun_close_hit_triggered"
display_name = "Close Hit"
description = "Shotgun pellets from the same shot deal bonus final damage when multiple base pellets hit the same target. The close-hit passive event still triggers inside the close-range threshold."
condition_type = "distance_threshold"
refresh_type = "reload"
ui_mode = "threshold"
