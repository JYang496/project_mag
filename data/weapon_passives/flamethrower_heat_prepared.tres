[gd_resource type="Resource" script_class="WeaponPassiveBranchDefinition" load_steps=2 format=3]

[ext_resource type="Script" path="res://data/WeaponPassiveBranchDefinition.gd" id="1_script"]

[resource]
script = ExtResource("1_script")
passive_id = "flamethrower_heat_prepared"
display_name = "Heat Prepared"
description = "Heat/Reload trigger. Accumulate weapon heat, then start a reload to gain a temporary damage boost; the boost applies after reload flow resolves."
condition_type = "heat_accumulation"
refresh_type = "reload_finished"
ui_mode = "progress"
