Project: project_mag
Task: Convert the legacy test folder reorganization plan into the next executable migration phase.

Environment:

- Project root: `D:\Godot Projects\project_mag`
- Shell: PowerShell
- Godot executable for future implementation: `D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe`

Current known state:

- `legacy test folder ` is still the active test/probe/benchmark folder.
- `docs/audits/world_test_reorganization_plan.md` already exists and should be treated as the current planning source.
- The full migration has a large `.tscn` and `legacy res test path ` path blast radius.

Goal:

- Do not move all tests.
- Select exactly one low-risk first migration slice and write an executable prompt for it.
- Prefer a slice with mostly self-contained files and few cross-references.

Writable scope:

- `docs/prompt/**`
- Optional `docs/audits/**` if the plan needs a small update

Forbidden scope:

- Do not move files out of `legacy test folder ` in this phase.
- Do not edit test `.gd`, `.tscn`, `.uid`, or `project.godot`.
- Do not create the full `tests/` tree unless it is documentation-only and justified.

Required workflow:

1. Run `git status --short`.
2. Read `docs/audits/world_test_reorganization_plan.md`.
3. Recount live state:
   - `rg --files legacy test folder`
   - `rg -n "legacy res test path " -S .`
4. Identify 2-3 candidate first slices, such as:
   - a small probe family
   - one benchmark family
   - one self-contained UI test family
5. Choose one first slice based on lowest reference risk.
6. Create a new implementation prompt under `docs/prompt/`, for example:
   `docs/prompt/project_structure_sequence_07_world_test_first_slice.txt`
7. The new prompt must include:
   - exact files allowed to move
   - exact destination
   - exact references expected to change
   - UID handling rules
   - validation commands, including the shell Godot check
   - rollback-avoidance rules: no `git reset`, no reverting unrelated dirty files

Validation:

- `git diff --check -- docs/prompt docs/audits`
- No Godot check is required if only prompt/docs files are edited.

Final report:

- Name the selected first migration slice.
- Link the new prompt file.
- State that no `legacy test folder` files were moved.
