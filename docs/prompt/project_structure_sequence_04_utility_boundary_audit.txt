Project: project_mag
Task: Audit Utility ownership boundaries and produce a staged reorganization proposal.

Environment:

- Project root: `D:\Godot Projects\project_mag`
- Shell: PowerShell
- This is a docs-only analysis task unless the user explicitly authorizes moves.

Current known state:

- `Utility/` contains mixed concerns: damage pipeline, hit/hurt boxes, status effects, reward/spawn/save helpers, area effects, and miscellaneous runtime helpers.
- `DisplaySettings` has already moved out of `Utility` into `autoload`.
- The project prefers concrete code-backed findings over generic architecture advice.

Goal:

- Produce a repo-grounded ownership audit for `Utility/`.
- Recommend staged moves, but do not move files in this phase.

Writable scope:

- `docs/audits/**`
- Optional `docs/design/**` only if promoting a final architecture contract

Forbidden scope:

- Do not edit `.gd`, `.tscn`, `.tres`, `.uid`, `project.godot`, or assets.
- Do not move `Utility` files.
- Do not update references.

Required workflow:

1. Run `git status --short`.
2. Inventory Utility:
   - `rg --files Utility`
   - group by subfolder and extension
3. For each root-level `Utility/*.gd` or `Utility/*.tscn`, search references:
   - `rg -n "res://Utility/<file>|preload\\(\"res://Utility|load\\(\"res://Utility|Utility/" -S .`
4. Classify each Utility item into one of:
   - Keep in `Utility` as true cross-domain primitive.
   - Move to `autoload` because it is a singleton/runtime service.
   - Move under a gameplay domain such as `World`, `Player`, `Npc`, `Objects`, or `data`.
   - Keep temporarily because move risk is high.
5. For each proposed move, include blast radius, exact reference patterns, and validation command.
6. Save the audit as `docs/audits/utility_boundary_audit.md`.

Validation:

- `git diff --check -- docs/audits/utility_boundary_audit.md`
- No Godot check is required if this remains docs-only.

Final report:

- State that no Utility files were moved.
- Link the audit file.
- Summarize the highest-value future moves and the risky moves to defer.
