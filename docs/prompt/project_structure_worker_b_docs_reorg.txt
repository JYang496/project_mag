Project: project_mag
Task: Reorganize documentation into stable management folders.

Context:
- Current project root is `D:\Godot Projects\project_mag`.
- The existing `docs/` folder mixes design docs, audit docs, generated HTML reports, DPS reports, and prompt files.
- The repo may already contain unrelated deleted or modified docs files. Do not restore or delete unrelated files unless this task explicitly requires it.
- This checkout currently has many dirty docs entries, including deleted reports and untracked prompt/audit files. Preserve that state unless a file is explicitly in this task's planned move set.
- Do not assume `godot` is on PATH. If this docs task unexpectedly touches a Godot file, use `D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe` for the final Godot check.
- Godot MCP may report a different Godot version than the shell executable. Do not use MCP as the only validation path for any `.gd`, `.tscn`, `.tres`, or `project.godot` change.

Writable scope:
- `docs/**`

Forbidden scope:
- Any code, scene, resource, asset, test, or project setting outside `docs/**`
- Do not modify files under `docs/prompt/**` except to add or update a README for prompt organization if needed

Target structure:
- `docs/design/` for durable design contracts and implementation designs.
- `docs/audits/` for code or UX audit findings.
- `docs/reports/` for generated or snapshot reports, especially HTML.
- `docs/reports/dps/` for DPS benchmark reports, if moving `docs/dps_reports/**`.
- `docs/prompt/` for worker and integrator prompt artifacts.

Suggested moves:
- `docs/cell_task_module_design.md` -> `docs/design/cell_task_module_design.md`
- `docs/cell_task_combat_hud_solution.md` -> `docs/design/cell_task_combat_hud_solution.md`
- `docs/player_operation_guidance_audit.md` -> `docs/audits/player_operation_guidance_audit.md`
- `docs/weapon_module_audit.html` -> `docs/reports/weapon_module_audit.html`
- `docs/weapon_system_positioning_advice.html` -> `docs/reports/weapon_system_positioning_advice.html`
- `docs/module_icon_preview.html` -> `docs/reports/module_icon_preview.html`
- `docs/dps_reports/**` -> `docs/reports/dps/**`
- Consider renaming `docs/module prompt.txt` to a clearer prompt filename under `docs/prompt/` only if it is actually a reusable prompt.

Required workflow:
1. Run `git status --short docs` first and preserve user changes.
2. Search repo-wide for references to docs paths before moving files.
3. Move documentation using Godot-safe path awareness where relevant, but do not change gameplay resources.
4. Update any markdown links or references that point to moved docs.
5. Add `docs/README.md` if absent. It should explain the folder purposes briefly.

Validation:
- Run `rg -n "docs/(cell_task_module_design|cell_task_combat_hud_solution|player_operation_guidance_audit|weapon_module_audit|weapon_system_positioning_advice|module_icon_preview|dps_reports)" -S .`.
- Run `git diff --check -- docs`.
- Godot check is optional for docs-only moves unless a `.tres`, `.tscn`, `.gd`, or `project.godot` file is touched.
- If Godot check is required, run `& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' --headless --path . --check-only --quit`.

Final report:
- List moved, renamed, added, and deleted docs files.
- State which existing references were updated.
- State whether any old docs paths remain and why.
- State which docs changes were pre-existing and intentionally preserved.
- Include validation commands and results.
