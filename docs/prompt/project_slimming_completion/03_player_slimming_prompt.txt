你在 Windows 本地 Godot 项目 `D:\Godot Projects\project_mag` 工作。

阶段 3：继续 Player 瘦身。

目标：继续降低 `Player/Mechas/scripts/Player.gd` 职责，但必须先用 focused gate 保护行为。

开始前必须执行：

```powershell
git status --short --branch
git rev-parse HEAD
```

开始前阅读：

- `docs/plans/project_slimming/player_progress.md`
- `docs/plans/project_slimming/overall_progress.md`
- `Player/Mechas/scripts/Player.gd`
- `Player/Mechas/scripts/player_camera_system.gd`
- `Player/Mechas/scripts/player_damage_reaction_system.gd`
- `tests/scenes/player/**`
- `tests/headless/player/**`

不要为了行数裸拆。每批只迁移一个责任块。

## 推荐顺序

1. camera/rest-area control
2. incoming elemental damage reaction
3. damage profile/pipeline ownership

## 每个 Player 批次固定流程

1. 搜索调用方：

```powershell
rg -n "<target_method_or_field>" Player tests World UI
```

2. 记录修改前指标：

```powershell
(Get-Content -Encoding UTF8 -Path Player/Mechas/scripts/Player.gd).Count
```

3. 如果没有 focused gate，先新增或扩展 `tests/scenes/player/**.tscn`。
4. 迁移一个责任块，不迁移多个责任块。
5. 保留公开兼容门面，直到调用方迁移完成。
6. 不让新系统反向读取或写入 `Player.gd` 私有状态；必要状态通过明确输入、配置或窄方法传入。
7. 验证：

```powershell
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' --headless --path . --check-only --quit
powershell -NoProfile -ExecutionPolicy Bypass -File tests/infrastructure/run_selected_tests.ps1 -ChangedPath 'Player/Mechas/scripts/Player.gd' -GodotPath 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' -Jobs 2 -OutputRoot 'test-results\player-slimming-<batch>'
git diff --check
```

8. 更新：
   - `docs/plans/project_slimming/player_progress.md`
   - `docs/plans/project_slimming/overall_progress.md`

## 验收

- `Player.gd` 减少一个真实职责。
- 新系统不反向读取 Player 私有状态。
- 行为 gate PASS。
- 最终报告包含行数变化、职责变化、验证结果和剩余风险。
