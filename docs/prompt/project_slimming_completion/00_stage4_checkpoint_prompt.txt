你在 Windows 本地 Godot 项目 `D:\Godot Projects\project_mag` 工作。

阶段 0：固定当前 Stage 4 checkpoint。

目标：确认上一阶段改动是完整、干净、可继续开发的基线。当前目标不是采用 `ponytail` 的进一步裁剪建议，而是先把项目瘦身计划收口。

开始前必须执行：

```powershell
git status --short --branch
git rev-parse HEAD
git diff --stat
```

不要回滚或覆盖其他人的改动。当前工作区可能已经包含上一阶段 Stage 4 的未提交改动；先识别它们，不要混入无关改动。

开始前阅读：

- `docs/plans/project_slimming/overall_progress.md`
- `docs/plans/project_slimming/test_infrastructure_progress.md`
- `docs/plans/project_slimming/player_progress.md`
- `docs/plans/project_slimming/ui_progress.md`
- `docs/plans/project_slimming/asset_pipeline_progress.md`
- `tests/README.md`

## 执行

1. 确认当前改动只属于 Stage 4：
   - Player 死代码清理
   - 孤儿 `.import` 删除
   - manifest 扩到 19 个 gate
   - Windows PowerShell 测试基础设施兼容修复
   - progress/handoff 文档同步
2. 运行验证：

```powershell
git diff --check
powershell -NoProfile -ExecutionPolicy Bypass -File tests/infrastructure/tests/test_selection_test.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File tests/infrastructure/tests/test_worker_test.ps1 -GodotPath 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe'
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' --headless --path . --check-only --quit
```

3. 确认没有残留 headless Godot worker 进程：

```powershell
Get-CimInstance Win32_Process -Filter "name = 'godot.windows.opt.tools.64.exe'" | Select-Object ProcessId,CommandLine
```

4. 如果用户允许提交，则提交为一个独立 checkpoint；如果不允许提交，只在最终报告中标记为 Stage 4 baseline。

## 验收

- 工作区改动边界清楚。
- 所有验证 PASS。
- 没有残留 headless Godot worker 进程。
- 更新 `docs/plans/project_slimming/overall_progress.md` 中的 Stage 4 baseline 状态（如需要）。
