你在 Windows 本地 Godot 项目 `D:\Godot Projects\project_mag` 工作。

阶段 7：最终收口报告。

目标：在全部瘦身阶段完成后，生成可审阅、可交接的完成报告，并明确哪些 `ponytail` 建议可以在计划完成后另行考虑。

开始前必须执行：

```powershell
git status --short --branch
git rev-parse HEAD
```

开始前阅读：

- `docs/plans/project_slimming/overall_progress.md`
- `docs/plans/project_slimming/ui_progress.md`
- `docs/plans/project_slimming/player_progress.md`
- `docs/plans/project_slimming/test_infrastructure_progress.md`
- `docs/plans/project_slimming/startup_progress.md`
- `docs/plans/project_slimming/asset_pipeline_progress.md`
- `tests/infrastructure/test_manifest.json`

## 执行

更新或新增：

- `docs/plans/project_slimming/overall_progress.md`
- 各范围 progress
- `docs/reports/project_slimming_completion_report.md`

最终报告必须包含：

- 完成批次列表
- `UI.gd` / `Player.gd` 修改前后行数和职责变化
- manifest 最终条目数和状态语义
- Startup benchmark 对比
- 资源管线实际删除/保留项
- 所有验证命令和结果
- 仍保留的风险
- 哪些 `ponytail` 建议可以在计划完成后另行考虑

最终验证：

```powershell
git diff --check
& 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' --headless --path . --check-only --quit
powershell -NoProfile -ExecutionPolicy Bypass -File tests/infrastructure/run_test_workers.ps1 -GodotPath 'D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' -Jobs 2 -OutputRoot 'test-results\project-slimming-final'
```

## 验收

- 完成报告存在且可独立审阅。
- 报告中的验证命令有实际结果。
- 剩余风险不是笼统描述，必须指向具体文件、测试或后续批次。
