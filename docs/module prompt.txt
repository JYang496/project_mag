【状态说明】这是可复用的操作模板，不是项目现状文档。报告生成器会创建 `docs/reports/weapon_module_audit.html`；该 HTML 是生成物，可能尚不存在，也不应作为长期设计真相。

你是 Godot 4/GDScript 开发者。请基于现有项目武器模组风格，为以下的每一个特色，单独设计并实现以下“新武器模组”。


【项目约束（必须遵守）】
1. 新模组继承 `Module`（`Player/Weapons/Modules/wmod_base.gd`）。
2. 文件命名：
   - 脚本：`Player/Weapons/Modules/wmod_<effect>_<tag>.gd`
   - 场景：`Player/Weapons/Modules/wmod_<effect>_<tag>.tscn`
3. 场景结构必须复用 `wmod_base.tscn`，并设置：
   - `script`
   - `Sprite.texture`（并且默认不可视）
   - 必要时设置 `required_weapon_traits` / `required_delivery_types` / `required_weapon_capabilities`
   - 事件模组必须设置 `required_hooks`
   - 描述性标签使用 `module_tags`，不得将标签作为兼容条件
4. 若是纯数值模组：优先实现 `configure_stat_modifiers()`，通过 `stat_multipliers/stat_additives` 修改属性。
5. 若是命中触发模组：继承 `OnHitModule`（`Player/Weapons/Modules/wmod_on_hit_base.gd`）并实现 `apply_on_hit(source_weapon, target)`。注册与反注册由基类统一处理；仅在需要额外生命周期逻辑时覆盖 `_enter_tree/_ready/_exit_tree`，并调用对应的 `super`。
6. 所有逻辑都要做空值与接口检查（`is_instance_valid`、`has_method`）。
7. 等级成长必须兼容 `module_level`（1~3），优先使用 `get_effective_multiplier/get_effective_additive` 或 `lv1/lv2/lv3` 参数。
8. 不要破坏现有接口和模块兼容性文案格式（如 `Requires one of: ...`）。
9. 每次新增或修改模组后，必须运行 `tools/generate_weapon_module_report.gd`，同步更新 `docs/reports/weapon_module_audit.html`。

【设计目标】
- 模组名称：{{MODULE_NAME}}
- 模组定位：{{DESIGN_GOAL}}（如：冻结流/热量流/高频命中/投射物）
- 触发方式：{{TRIGGER_TYPE}}（stat 或 on_hit）
- 目标武器特性限制：{{REQUIRED_TRAITS}}（如 freeze / heat / projectile）
- 预期强度：{{POWER_BUDGET}}（早中后期）
- 是否需要 fuse 联动：{{FUSE_SCALING}}

【输出内容（按顺序）】
1. 先给“设计说明”（机制、适配武器、强弱项、平衡风险）。
2. 输出完整 `.gd` 代码。
3. 输出完整 `.tscn` 内容（含 base scene 继承、脚本、图标、导出参数）。
4. 列出需要补充的本地化 key（`data/localization/game_content.csv` 的 `module.<id>.name`）。
5. 给出 5 条测试清单（兼容性、数值生效、边界条件、多模组叠加、回归风险）。

【代码风格】
- 保持和现有模组一致：短注释、早返回、命名清晰。
- 不引入无关系统改动，不修改其他武器脚本，除非必要并明确说明原因。
